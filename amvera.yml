meta:
  environment: node
  toolchain:
    name: npm
    version: 20

# Build section removed as it contained invalid fields. Amvera handles npm install automatically.


run:
  command: node streamlume_server.js

persistence:
  - path: database.sqlite
    shared: false
