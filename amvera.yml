meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  # Принудительная пересборка нативных модулей (better-sqlite3) под Linux
  noInstall: false

run:
  command: node streamlume_server.js

persistence:
  - path: database.sqlite
    shared: false
